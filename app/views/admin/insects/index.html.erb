<h1>Insects</h1>

<%= link_to "New insect", new_admin_insect_path %>

<div>
  <% @insects.each do |insect| %>
    <div>
      <%= link_to insect.name, admin_insect_path(insect) %>
    </div>
  <% end %>
</div>
